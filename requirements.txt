# Версии закреплены точно. На них собрано и проверено всё, что заявлено в
# README: метрики, тесты, витрина. Нестрогие ограничения (>=) означали бы,
# что сборка на сервере ставит пакеты новее протестированных, и однажды
# деплой упадёт на несовместимости, которой у нас никогда не было локально.
numpy==2.4.6
pandas==3.0.6
scikit-learn==1.9.1
lightgbm==4.7.0
shap==0.51.0
joblib==1.6.0
fastapi==0.141.1
uvicorn[standard]==0.53.0
pydantic==2.13.5
